{{- define "shared.service" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-svc
spec:
  ports:
    - port: {{ .Values.servicePort }}
      protocol: TCP
      targetPort: {{ .Values.serviceTargetPort }}
  selector:
    app: {{ .Chart.Name }}
{{- end -}}