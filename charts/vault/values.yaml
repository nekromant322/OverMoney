serviceAccountNamespace: default
secretProviderClass:
  name: vault-prod
  namespace: default
  roleName: prod
  secretName: prod
  objects:
    - objectName: "wit-ai-token"
      secretPath: "secret/prod"
      secretKey: "wit-ai-token"
    - objectName: "kafka-ui-user"
      secretPath: "secret/prod"
      secretKey: "kafka-ui-user"
    - objectName: "kafka-ui-password"
      secretPath: "secret/prod"
      secretKey: "kafka-ui-password"
    - objectName: "grafana-password"
      secretPath: "secret/prod"
      secretKey: "grafana-password"
    - objectName: "internal-key-header"
      secretPath: "secret/prod"
      secretKey: "internal-key-header"
    - objectName: "jwt-access-key"
      secretPath: "secret/prod"
      secretKey: "jwt-access-key"
    - objectName: "jwt-refresh-key"
      secretPath: "secret/prod"
      secretKey: "jwt-refresh-key"
    - objectName: "orchestrator-db-password"
      secretPath: "secret/prod"
      secretKey: "orchestrator-db-password"
    - objectName: "orchestrator-db-port"
      secretPath: "secret/prod"
      secretKey: "orchestrator-db-port"
    - objectName: "orchestrator-db-user"
      secretPath: "secret/prod"
      secretKey: "orchestrator-db-user"
    - objectName: "telegram-bot-db-password"
      secretPath: "secret/prod"
      secretKey: "telegram-bot-db-password"
    - objectName: "telegram-bot-db-user"
      secretPath: "secret/prod"
      secretKey: "telegram-bot-db-user"
    - objectName: "telegram-bot-token"
      secretPath: "secret/prod"
      secretKey: "telegram-bot-token"